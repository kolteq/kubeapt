ARG GO_VERSION=1.25

FROM golang:${GO_VERSION}-bookworm AS builder
WORKDIR /src

COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN CGO_ENABLED=0 go build -trimpath -ldflags="-s -w" -o /out/kubeapt ./main.go

FROM gcr.io/distroless/static-debian12

COPY --from=builder /out/kubeapt /usr/local/bin/kubeapt

RUN kubeapt bundles download kolteq-best-practices --version v0.1.0
RUN kubeapt bundles download pod-secuity-admission --version v1.34.0
RUN kubeapt policies download --version v0.1.0

ENTRYPOINT ["/usr/local/bin/kubeapt"]
